name: Privacy check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  scan:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Scan tracked files
        shell: pwsh
        run: ./scripts/privacy-check.ps1
